# =====================================================================
# MODELO. Este arquivo É versionado — ele nunca contém valor de verdade.
#
# Para usar: copie para `.env` e preencha lá.
# O `.env` está no `.gitignore` e nunca sai da sua máquina.
# =====================================================================

# ⚠️ ATENÇÃO ao prefixo, se o projeto usa Vite:
# tudo que começa com VITE_ é EMBUTIDO no pacote público, e qualquer pessoa
# que abrir o site consegue ler. Segredo NUNCA leva esse prefixo.

{{VARIAVEIS}}
